# Accepted differences for the formatter differential.  Format as in
# known-differences.txt.  A marker that matches nothing FAILS, so this
# list cannot go stale.

# (empty: every layout the header names now agrees with the reference)
